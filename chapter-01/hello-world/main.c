#include <stdio.h> // include information about standard library

main() // define a function named main that receives no argument values
{ 
    printf("hello, world\n"); // main calls library function printf to print this sequence of characters. NOTE: printf never supplys a new line automatically
}
